/* 
 * Generated by Overcast UTC 2024-03-12 08:53:16Z.
 * Changes to this class may cause incorrect behavior and will be lost if the scenario is re-activated.
 */
@istest
private class ovcSAPDeliveriesBlockingReasonTest {
    @istest
    private static void testovcSAPDeliveriesBlockingReason() {
        ovcSAPDeliveriesBlockingReason actual = new ovcSAPDeliveriesBlockingReason();
        system.assertNotEquals(null, actual.TVLST);
    }

    @istest
    private static void testTVLST() {
        ovcSAPDeliveriesBlockingReason.TVLST actual = new ovcSAPDeliveriesBlockingReason.TVLST();

        actual.MANDT = 'a';
        actual.SPRAS = 'a';
        actual.LIFSP = 'a';
        actual.VTEXT = 'a';
        system.assertEquals('a', actual.MANDT);
        system.assertEquals('a', actual.SPRAS);
        system.assertEquals('a', actual.LIFSP);
        system.assertEquals('a', actual.VTEXT);
    }
    @istest
    private static void testrunQuery() {
        ovcSAPDeliveriesBlockingReason request = testSetup();
        overcast.Query query1 = new overcast.Query();
        query1.limit_x = 10;
        query1.offset = 10;
        
        ovcSAPDeliveriesBlockingReason response = request.runQuery(query1);
        system.assertEquals('a', response.TVLST[0].MANDT);
        string requestid = request.runQueryAsync(query1, null, null, new Continuation(10));
    }

    private static ovcSAPDeliveriesBlockingReason testSetup() {
        overcast.TestingUtilities.SetupTestEnvironment('ovcSAPDeliveriesBlockingReason');
        ovcSAPDeliveriesBlockingReason request = new ovcSAPDeliveriesBlockingReason();
        ovcSAPDeliveriesBlockingReason.TVLST value1 = new ovcSAPDeliveriesBlockingReason.TVLST();
        value1.MANDT = 'a';
        request.TVLST.add(value1);

        string s = request.serialize();
        request.testResponse = '{"output":' + s + '}';
        return request;
    }


    @istest
    private static void testSerialize() {
        ovcSAPDeliveriesBlockingReason input = new ovcSAPDeliveriesBlockingReason();
        ovcSAPDeliveriesBlockingReason.TVLST value1 = new ovcSAPDeliveriesBlockingReason.TVLST();
        value1.MANDT = 'a';
        input.TVLST.add(value1);
        ovcSAPDeliveriesBlockingReason actual = (ovcSAPDeliveriesBlockingReason)JSON.deserialize(input.serialize(), ovcSAPDeliveriesBlockingReason.class);
        system.assertEquals('a', actual.TVLST[0].MANDT);
    }

    @istest
    private static void testDeserialize() {
        ovcSAPDeliveriesBlockingReason output = new ovcSAPDeliveriesBlockingReason();
        ovcSAPDeliveriesBlockingReason.TVLST value1 = new ovcSAPDeliveriesBlockingReason.TVLST();
        value1.MANDT = 'a';
        output.TVLST.add(value1);

        string s = output.serialize();
        ovcSAPDeliveriesBlockingReason actual = ovcSAPDeliveriesBlockingReason.deserialize('{"output":' + s + '}');
        system.assertEquals('a', actual.TVLST[0].MANDT);
    }

    @istest
    private static void testDeserializeError() {
        try {
            ovcSAPDeliveriesBlockingReason actual = ovcSAPDeliveriesBlockingReason.deserialize('{"error":["A test error"]}');
            system.assert(false, 'No exception thrown');
        } catch (ovcSAPDeliveriesBlockingReason.ovcSAPDeliveriesBlockingReasonException e) {
            system.assertEquals('A test error', e.getMessage());
            system.assertEquals(0, e.getBackendMessages().size());
        }

        try {
            ovcSAPDeliveriesBlockingReason actual = ovcSAPDeliveriesBlockingReason.deserialize('{"error":["A test error"],"backendErrorMessages":["A test message"]}');
            system.assert(false, 'No exception thrown');
        } catch (ovcSAPDeliveriesBlockingReason.ovcSAPDeliveriesBlockingReasonException e) {
            system.assertEquals('A test error', e.getMessage());
            system.assertEquals(1, e.getMessages().size());
            system.assertEquals('A test error', e.getMessages()[0]);
            system.assertEquals(1, e.getBackendMessages().size());
            system.assertEquals('A test message', e.getBackendMessages()[0]);
        }
    }

    @istest
    private static void testGetterSetter() {
        ovcSAPDeliveriesBlockingReason output = new ovcSAPDeliveriesBlockingReason();
        output.set('SAMPLE1', new map<string, object>{
            'FIELD1' => 'VALUE1'
        });
        
        system.assertEquals(null, output.get('NOVAL'));
        
        map<string, object> sample1 = (map<string, object>)output.get('SAMPLE1');
        system.assertNotEquals(null, sample1);
        system.assertEquals('VALUE1', sample1.get('FIELD1'));		
    }
    
    @istest
    private static void testLoadAsyncResponse() {
        ovcSAPDeliveriesBlockingReason output = new ovcSAPDeliveriesBlockingReason();
        output.loadAsyncResponse('{"output":{"SAMPLE1":{"FIELD1":"VALUE1"}}}');
        system.assertEquals(null, output.get('NOVAL'));
        map<string, object> sample1 = (map<string, object>)output.get('SAMPLE1');
        system.assertNotEquals(null, sample1);
        system.assertEquals('VALUE1', sample1.get('FIELD1'));

        overcast.Globals.IsOCF = true;
        ovcSAPDeliveriesBlockingReason output2 = new ovcSAPDeliveriesBlockingReason();
        output2.loadAsyncResponse('{"output":{"SAMPLE1":{"FIELD1":"VALUE1"}}}');
        overcast.Globals.IsOCF = false;
        system.assertEquals(null, output2.get('NOVAL'));
        map<string, object> sample2 = (map<string, object>)output2.get('SAMPLE1');
        system.assertNotEquals(null, sample2);
        system.assertEquals('VALUE1', sample2.get('FIELD1'));
    }

    @istest
    private static void testGetMetadata() {
        ovcSAPDeliveriesBlockingReason output = new ovcSAPDeliveriesBlockingReason();
        map<string, overcast.TypeInfo> metadata = output.getMetadata();
        
        set<string> keys = metadata.keySet();
        system.assert(keys.contains('TVLST'));
        
        overcast.TypeInfo first = metadata.get(output.getTopTypeName());
        system.assertNotEquals(null, first);
        system.assertEquals('TVLST', first.name);
    }

    @istest
    private static void testException() {
        ovcSAPDeliveriesBlockingReason.ovcSAPDeliveriesBlockingReasonException e = new ovcSAPDeliveriesBlockingReason.ovcSAPDeliveriesBlockingReasonException();
        e.setMessages(new List<String>{'M1', 'M2'});
        System.assertEquals(2, e.getMessages().size());
    }

    @istest
    private static void testErrorResponse() {
        try {
            ovcSAPDeliveriesBlockingReason.deserialize('');
            System.assert(false, 'Expected exception not thrown.');
        } catch (ovcSAPDeliveriesBlockingReason.ovcSAPDeliveriesBlockingReasonException e) {
            System.assert(true);
        }
        
        try {
            ovcSAPDeliveriesBlockingReason output = new ovcSAPDeliveriesBlockingReason();
            output.loadAsyncResponse('{"error":["ERROR1","ERROR2"]}');
            System.assert(false, 'Expected exception not thrown.');
        } catch (ovcSAPDeliveriesBlockingReason.ovcSAPDeliveriesBlockingReasonException e) {
            System.assertEquals('ERROR1;ERROR2', e.getMessage());
            System.assertEquals('ERROR1', e.getMessages()[0]);
        }
        
        try {
            ovcSAPDeliveriesBlockingReason output = new ovcSAPDeliveriesBlockingReason();
            output.loadAsyncResponse('{"backendMessage":"TEST RESULT"}');
            System.assert(false, 'Expected exception not thrown.');
        } catch (ovcSAPDeliveriesBlockingReason.ovcSAPDeliveriesBlockingReasonException e) {
            System.assert(true);
        }

        try {
            ovcSAPDeliveriesBlockingReason output = new ovcSAPDeliveriesBlockingReason();
            output.loadAsyncResponse('{"error":["ERROR1","ERROR2"],"backendErrorMessages":["A test message"]}');
            System.assert(false, 'Expected exception not thrown.');
        } catch (ovcSAPDeliveriesBlockingReason.ovcSAPDeliveriesBlockingReasonException e) {
            System.assertEquals('ERROR1;ERROR2', e.getMessage());
            System.assertEquals(2, e.getMessages().size());
            System.assertEquals('ERROR1', e.getMessages()[0]);
            System.assertEquals('ERROR2', e.getMessages()[1]);
            System.assertEquals(1, e.getBackendMessages().size());
            System.assertEquals('A test message', e.getBackendMessages()[0]);
        }

        overcast.Globals.IsOCF = true;
        
        try {
            ovcSAPDeliveriesBlockingReason.deserialize('');
            System.assert(false, 'Expected exception not thrown.');
        } catch (ovcSAPDeliveriesBlockingReason.ovcSAPDeliveriesBlockingReasonException e) {
            System.assert(true);
        }
        
        try {
            ovcSAPDeliveriesBlockingReason.deserialize('{"error":["ERROR1","ERROR2"]}');
            System.assert(false, 'Expected exception not thrown.');
        } catch (ovcSAPDeliveriesBlockingReason.ovcSAPDeliveriesBlockingReasonException e) {
            System.assertEquals('ERROR1;ERROR2', e.getMessage());
            System.assertEquals(0, e.getBackendMessages().size());
        }

        try {
            ovcSAPDeliveriesBlockingReason.deserialize('{"error":["ERROR1","ERROR2"],"backendErrorMessages":["A test message"]}');
            System.assert(false, 'Expected exception not thrown.');
        } catch (ovcSAPDeliveriesBlockingReason.ovcSAPDeliveriesBlockingReasonException e) {
            System.assertEquals('ERROR1;ERROR2', e.getMessage());
            System.assertEquals(2, e.getMessages().size());
            System.assertEquals('ERROR1', e.getMessages()[0]);
            System.assertEquals('ERROR2', e.getMessages()[1]);
            System.assertEquals(1, e.getBackendMessages().size());
            System.assertEquals('A test message', e.getBackendMessages()[0]);
        }
    }

    @istest
    private static void testGetBackendMessage() {
        ovcSAPDeliveriesBlockingReason output = new ovcSAPDeliveriesBlockingReason();
        output.loadAsyncResponse('{"output":{}, "backendMessage":"TEST RESULT"}');
        system.assertEquals('TEST RESULT', output.getBackendMessage());
    }

}