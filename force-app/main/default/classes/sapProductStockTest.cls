/* 
 * Generated by Overcast UTC 2024-03-12 09:51:42Z.
 * Changes to this class may cause incorrect behavior and will be lost if the scenario is re-activated.
 */
@istest
private class sapProductStockTest {
    @istest
    private static void testsapProductStock() {
        sapProductStock actual = new sapProductStock();
        system.assertNotEquals(null, actual.MMBE_MARD_V);
    }

    @istest
    private static void testMMBE_MARD_V() {
        sapProductStock.MMBE_MARD_V actual = new sapProductStock.MMBE_MARD_V();

        actual.MANDT = 'a';
        actual.MATNR = 'a';
        actual.WERKS = 'a';
        actual.LGORT = 'a';
        actual.LVORM = 'a';
        actual.LABST = 0;
        actual.UMLME = 0;
        actual.INSME = 0;
        actual.EINME = 0;
        actual.SPEME = 0;
        actual.RETME = 0;
        actual.LGPBE = 'a';
        actual.KLABS = 0;
        actual.KINSM = 0;
        actual.KEINM = 0;
        actual.KSPEM = 0;
        system.assertEquals('a', actual.MANDT);
        system.assertEquals('a', actual.MATNR);
        system.assertEquals('a', actual.WERKS);
        system.assertEquals('a', actual.LGORT);
        system.assertEquals('a', actual.LVORM);
        system.assertEquals(0, actual.LABST);
        system.assertEquals(0, actual.UMLME);
        system.assertEquals(0, actual.INSME);
        system.assertEquals(0, actual.EINME);
        system.assertEquals(0, actual.SPEME);
        system.assertEquals(0, actual.RETME);
        system.assertEquals('a', actual.LGPBE);
        system.assertEquals(0, actual.KLABS);
        system.assertEquals(0, actual.KINSM);
        system.assertEquals(0, actual.KEINM);
        system.assertEquals(0, actual.KSPEM);
    }
    @istest
    private static void testrunQuery() {
        sapProductStock request = testSetup();
        overcast.Query query1 = new overcast.Query();
        query1.limit_x = 10;
        query1.offset = 10;
        
        sapProductStock response = request.runQuery(query1);
        system.assertEquals('a', response.MMBE_MARD_V[0].MANDT);
        string requestid = request.runQueryAsync(query1, null, null, new Continuation(10));
    }

    private static sapProductStock testSetup() {
        overcast.TestingUtilities.SetupTestEnvironment('sapProductStock');
        sapProductStock request = new sapProductStock();
        sapProductStock.MMBE_MARD_V value1 = new sapProductStock.MMBE_MARD_V();
        value1.MANDT = 'a';
        request.MMBE_MARD_V.add(value1);

        string s = request.serialize();
        request.testResponse = '{"output":' + s + '}';
        return request;
    }


    @istest
    private static void testSerialize() {
        sapProductStock input = new sapProductStock();
        sapProductStock.MMBE_MARD_V value1 = new sapProductStock.MMBE_MARD_V();
        value1.MANDT = 'a';
        input.MMBE_MARD_V.add(value1);
        sapProductStock actual = (sapProductStock)JSON.deserialize(input.serialize(), sapProductStock.class);
        system.assertEquals('a', actual.MMBE_MARD_V[0].MANDT);
    }

    @istest
    private static void testDeserialize() {
        sapProductStock output = new sapProductStock();
        sapProductStock.MMBE_MARD_V value1 = new sapProductStock.MMBE_MARD_V();
        value1.MANDT = 'a';
        output.MMBE_MARD_V.add(value1);

        string s = output.serialize();
        sapProductStock actual = sapProductStock.deserialize('{"output":' + s + '}');
        system.assertEquals('a', actual.MMBE_MARD_V[0].MANDT);
    }

    @istest
    private static void testDeserializeError() {
        try {
            sapProductStock actual = sapProductStock.deserialize('{"error":["A test error"]}');
            system.assert(false, 'No exception thrown');
        } catch (sapProductStock.sapProductStockException e) {
            system.assertEquals('A test error', e.getMessage());
            system.assertEquals(0, e.getBackendMessages().size());
        }

        try {
            sapProductStock actual = sapProductStock.deserialize('{"error":["A test error"],"backendErrorMessages":["A test message"]}');
            system.assert(false, 'No exception thrown');
        } catch (sapProductStock.sapProductStockException e) {
            system.assertEquals('A test error', e.getMessage());
            system.assertEquals(1, e.getMessages().size());
            system.assertEquals('A test error', e.getMessages()[0]);
            system.assertEquals(1, e.getBackendMessages().size());
            system.assertEquals('A test message', e.getBackendMessages()[0]);
        }
    }

    @istest
    private static void testGetterSetter() {
        sapProductStock output = new sapProductStock();
        output.set('SAMPLE1', new map<string, object>{
            'FIELD1' => 'VALUE1'
        });
        
        system.assertEquals(null, output.get('NOVAL'));
        
        map<string, object> sample1 = (map<string, object>)output.get('SAMPLE1');
        system.assertNotEquals(null, sample1);
        system.assertEquals('VALUE1', sample1.get('FIELD1'));		
    }
    
    @istest
    private static void testLoadAsyncResponse() {
        sapProductStock output = new sapProductStock();
        output.loadAsyncResponse('{"output":{"SAMPLE1":{"FIELD1":"VALUE1"}}}');
        system.assertEquals(null, output.get('NOVAL'));
        map<string, object> sample1 = (map<string, object>)output.get('SAMPLE1');
        system.assertNotEquals(null, sample1);
        system.assertEquals('VALUE1', sample1.get('FIELD1'));

        overcast.Globals.IsOCF = true;
        sapProductStock output2 = new sapProductStock();
        output2.loadAsyncResponse('{"output":{"SAMPLE1":{"FIELD1":"VALUE1"}}}');
        overcast.Globals.IsOCF = false;
        system.assertEquals(null, output2.get('NOVAL'));
        map<string, object> sample2 = (map<string, object>)output2.get('SAMPLE1');
        system.assertNotEquals(null, sample2);
        system.assertEquals('VALUE1', sample2.get('FIELD1'));
    }

    @istest
    private static void testGetMetadata() {
        sapProductStock output = new sapProductStock();
        map<string, overcast.TypeInfo> metadata = output.getMetadata();
        
        set<string> keys = metadata.keySet();
        system.assert(keys.contains('MMBE_MARD_V'));
        
        overcast.TypeInfo first = metadata.get(output.getTopTypeName());
        system.assertNotEquals(null, first);
        system.assertEquals('MMBE_MARD_V', first.name);
    }

    @istest
    private static void testException() {
        sapProductStock.sapProductStockException e = new sapProductStock.sapProductStockException();
        e.setMessages(new List<String>{'M1', 'M2'});
        System.assertEquals(2, e.getMessages().size());
    }

    @istest
    private static void testErrorResponse() {
        try {
            sapProductStock.deserialize('');
            System.assert(false, 'Expected exception not thrown.');
        } catch (sapProductStock.sapProductStockException e) {
            System.assert(true);
        }
        
        try {
            sapProductStock output = new sapProductStock();
            output.loadAsyncResponse('{"error":["ERROR1","ERROR2"]}');
            System.assert(false, 'Expected exception not thrown.');
        } catch (sapProductStock.sapProductStockException e) {
            System.assertEquals('ERROR1;ERROR2', e.getMessage());
            System.assertEquals('ERROR1', e.getMessages()[0]);
        }
        
        try {
            sapProductStock output = new sapProductStock();
            output.loadAsyncResponse('{"backendMessage":"TEST RESULT"}');
            System.assert(false, 'Expected exception not thrown.');
        } catch (sapProductStock.sapProductStockException e) {
            System.assert(true);
        }

        try {
            sapProductStock output = new sapProductStock();
            output.loadAsyncResponse('{"error":["ERROR1","ERROR2"],"backendErrorMessages":["A test message"]}');
            System.assert(false, 'Expected exception not thrown.');
        } catch (sapProductStock.sapProductStockException e) {
            System.assertEquals('ERROR1;ERROR2', e.getMessage());
            System.assertEquals(2, e.getMessages().size());
            System.assertEquals('ERROR1', e.getMessages()[0]);
            System.assertEquals('ERROR2', e.getMessages()[1]);
            System.assertEquals(1, e.getBackendMessages().size());
            System.assertEquals('A test message', e.getBackendMessages()[0]);
        }

        overcast.Globals.IsOCF = true;
        
        try {
            sapProductStock.deserialize('');
            System.assert(false, 'Expected exception not thrown.');
        } catch (sapProductStock.sapProductStockException e) {
            System.assert(true);
        }
        
        try {
            sapProductStock.deserialize('{"error":["ERROR1","ERROR2"]}');
            System.assert(false, 'Expected exception not thrown.');
        } catch (sapProductStock.sapProductStockException e) {
            System.assertEquals('ERROR1;ERROR2', e.getMessage());
            System.assertEquals(0, e.getBackendMessages().size());
        }

        try {
            sapProductStock.deserialize('{"error":["ERROR1","ERROR2"],"backendErrorMessages":["A test message"]}');
            System.assert(false, 'Expected exception not thrown.');
        } catch (sapProductStock.sapProductStockException e) {
            System.assertEquals('ERROR1;ERROR2', e.getMessage());
            System.assertEquals(2, e.getMessages().size());
            System.assertEquals('ERROR1', e.getMessages()[0]);
            System.assertEquals('ERROR2', e.getMessages()[1]);
            System.assertEquals(1, e.getBackendMessages().size());
            System.assertEquals('A test message', e.getBackendMessages()[0]);
        }
    }

    @istest
    private static void testGetBackendMessage() {
        sapProductStock output = new sapProductStock();
        output.loadAsyncResponse('{"output":{}, "backendMessage":"TEST RESULT"}');
        system.assertEquals('TEST RESULT', output.getBackendMessage());
    }

}